import ComingSoon from "@/components/ComingSoon";

export default function EvidenceGraphPage() {
  return (
    <ComingSoon
      title="Evidence Graph is not yet implemented"
      description="This view will visualise evidence relationships (events, trust changes, and decisions linked to an incident) as a graph. It has no backend endpoint or data model yet — nothing below is real data."
    />
  );
}
